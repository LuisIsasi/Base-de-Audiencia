; ==================================
;  flower celery monitor
; ==================================

[program:celery-flower-b]
command=/data/local/apps/audb-b/.venv/audb-b/bin/python /data/local/apps/audb-b/src/manage.py celery flower
    -A audb
    --conf=/data/shared/audb-system/system-files/flower/flowerconfig_b.py

environment=PYTHONPATH="/data/local/apps/audb-b/src:$PYTHONPATH",DJANGO_SETTINGS_MODULE="audb.settings.prod_b"
directory=/data/local/apps/audb-b/src
user=deploy
numprocs=1
stdout_logfile=/data/local/log/celery/flower-b.log
stderr_logfile=/data/local/log/celery/flower-b.log
autostart=true
autorestart=true
startsecs=10

; Need to wait for currently executing tasks to finish at shutdown.
; Increase this if you have very long running tasks.
stopwaitsecs = 600

; When resorting to send SIGKILL to the program to terminate it
; send SIGKILL to its whole process group instead,
; taking care of its children as well.
killasgroup=true

; Set Celery priority higher than default (999)
; so, if rabbitmq is supervised, it will start first.
priority=1000
